/**
 * Thin re-export so existing `@/components/color-picker/color-picker` import
 * paths continue to work after the implementation moved to
 * `@/components/ui/color-picker`.
 */
export { ColorPicker } from '@/components/ui/color-picker'
